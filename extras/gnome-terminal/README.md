## Gnome Terminal profile (Ubuntu only)

Optional theme settings for Gnome Terminal.

Apply manually:
```bash
dconf load /org/gnome/terminal/ < gnome-terminal-profiles.dconf
```

This is Linux-only and intentionally not part of the Ansible playbook.
